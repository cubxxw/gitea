interface:
  display_name: "Gitea"
  short_description: "Repo-specific patterns and workflows for gitea"
  default_prompt: "Use the gitea repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true